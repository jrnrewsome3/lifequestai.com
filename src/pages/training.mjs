import {url, SITE} from '../config.mjs';
import {esc} from '../lib/components.mjs';
import {photoUrl, photoAlt} from '../data/photos.mjs';
import {LAUNCH, GROUPS, MODULES, PROMPTS, STARTER_PROMPT} from '../data/training.mjs';
const interest = (group='') => url('/ai-training/join/') + (group ? '?group='+encodeURIComponent(group) : '');
const link = (href,label,kind='primary') => `<a class="btn btn-${kind}" href="${href}"${href.endsWith('.txt') ? ' download' : ''}>${label}</a>`;
const intro = (eyebrow,title,copy) => `<p class="eyebrow">${eyebrow}</p><h1>${title}</h1><p class="lede">${copy}</p>`;
const photo = key => `<img src="${photoUrl(key,1000,1100)}" alt="${photoAlt(key)}" width="1000" height="1100" fetchpriority="high">`;
const band = (group='') => `<section class="tr-band"><div class="wrap tr-band-in"><div><p class="eyebrow">Your work. Your learning group.</p><h2>Let’s bring the right 10 people together.</h2><p>Join the interest list. We’ll confirm the class details before you decide to enroll.</p></div>${link(interest(group),'Find my learning group')}</div></section>`;
const process = () => `<section class="sec"><div class="wrap"><div class="sec-head"><p class="eyebrow">How a class comes together</p><h2>10 people. Similar work. Useful practice.</h2><p class="lede">We’re aiming to begin in ${LAUNCH.month}. Each class forms around people with related work needs.</p></div><ol class="tr-steps"><li><span>01</span><h3>Tell us what you do</h3><p>Choose a group and share one task you want AI to help with.</p></li><li><span>02</span><h3>We match the group</h3><p>When ${LAUNCH.minimum} interested people with similar needs come together, we organize a class.</p></li><li><span>03</span><h3>You decide to join</h3><p>We confirm the date, format, length, and price. You decide whether it works for you.</p></li></ol></div></section>`;
const syllabus = () => `<section class="sec tint" id="class"><div class="wrap tr-syllabus"><div><p class="eyebrow">The first class</p><h2>${LAUNCH.title}</h2><p class="lede">A beginner-friendly, guided session built around a task you recognize from your own work.</p><p>Practice turning an idea into a useful draft, checking the result, and saving a template you can use again.</p><p class="tr-note">No coding required. Class length, delivery format, tools, and price will be confirmed with each group.</p></div><ol class="tr-modules">${MODULES.map(([t,d],i)=>`<li><span>${String(i+1).padStart(2,'0')}</span><div><h3>${t}</h3><p>${d}</p></div></li>`).join('')}</ol></div></section>`;
const sample = () => `<section class="sec"><div class="wrap tr-sample"><div><p class="eyebrow">Try a small lesson now</p><h2>A better follow-up starts with a better brief.</h2><p class="lede">Copy this fictional example into an AI tool you’re allowed to use. Read the draft before you use it.</p><ol><li>Check that every fact comes from the notes.</li><li>Remove invented dates, prices, or promises.</li><li>Make the next step clear and edit it into your voice.</li></ol>${link(url('/resources/ai-starter-kit/'),'Get all 25 practice prompts','ghost')}</div><div class="tr-prompt"><p class="eyebrow">A ready-to-try prompt</p><pre id="sample-prompt">${esc(STARTER_PROMPT)}</pre><button class="btn btn-primary" type="button" data-copy="sample-prompt">Copy practice prompt</button><p class="tr-note" data-copy-status role="status"></p></div></div></section>`;
export function trainingHome(){return `<div class="training"><section class="tr-hero"><div class="wrap tr-hero-grid"><div class="tr-hero-copy">${intro('October 2026 · Interest list open','Learn AI with people who do work like yours.','Bring one task you want to make easier. Learn with a group that understands your work, and build something you can use again.')}<div class="btn-row">${link(interest(),'Find my learning group')}${link('#groups','Explore the groups','ghost')}</div><p class="tr-note">A class forms when 10 people with similar needs are interested. No payment or commitment to join the list.</p></div><div class="tr-hero-visual">${photo('hero')}<div class="tr-photo-note"><strong>Practical work. Shared context.</strong><span>Small-group AI learning, built around you.</span></div></div></div></section><section class="tr-strip"><div class="wrap"><span>Beginners welcome</span><span>Work on a familiar task</span><span>Keep a reusable template</span><span>Review every AI draft</span></div></section><section class="sec" id="groups"><div class="wrap"><div class="sec-head"><p class="eyebrow">Find your people</p><h2>Choose the group closest to your work.</h2><p class="lede">The examples change with the group. The goal stays the same: make AI useful in a routine you already have.</p></div><div class="tr-groups">${GROUPS.map((g,i)=>`<a class="tr-group" href="${url('/ai-training/'+g.slug+'/')}"><span class="tr-index">0${i+1}</span><div><h3>${g.name}</h3><p>${g.examples.map(e=>e[0]).join(' · ')}</p><span class="tr-text-link">See what you could work on <span aria-hidden="true">↗</span></span></div></a>`).join('')}</div><p class="tr-note tr-other">Don’t see your field? ${link(interest('other'),'Tell us what you do','quiet')} We’ll look for a good match.</p></div></section>${process()}${syllabus()}${sample()}<section class="sec tint"><div class="wrap tr-faq"><div><p class="eyebrow">Before you join</p><h2>A few useful answers.</h2></div><div>${[
['Is there a confirmed October class date?','October 2026 is our target launch window. We organize each class when 10 people with similar work needs are interested, then confirm a schedule with that group. A date is not guaranteed by joining the list.'],
['What does it cost?','Pricing is not set yet. You’ll receive the price, date, duration, format, and tool requirements before deciding to enroll. Joining the interest list costs nothing and does not reserve a paid seat.'],
['Will the class be online or in person?','Tell us your preference, location, time zone, and availability on the interest form. We’ll use that information when organizing the group.'],
['Can colleagues join together?','Yes. Each person should join the interest list, or a team organizer can tell us how many people may be interested. We’ll confirm individual interest before counting an entire team toward a class.'],
['What should I bring?','One task you want to improve, and fictional or non-sensitive examples you are permitted to use. We’ll confirm any account or software requirements in advance.'],
['What happens to my information?','We use your contact details and work preferences to match a learning group and follow up about classes. This form does not sign you up for a newsletter. Please don’t include client, student, financial, or other confidential information.']
].map(([q,a])=>`<details><summary>${q}</summary><p>${a}</p></details>`).join('')}</div></div></section>${band()}</div>`;}
export function audiencePage(g){return `<div class="training"><section class="tr-hero"><div class="wrap tr-hero-grid"><div class="tr-hero-copy"><a class="tr-back" href="${url('/ai-training/')}">← All learning groups</a>${intro(g.name,g.headline,g.intro)}<div class="btn-row">${link(interest(g.slug),'Join this interest list')}${link('#practice','Explore the practice','ghost')}</div><p class="tr-note">Targeting ${LAUNCH.month} · Class forms at 10 interested people with similar needs.</p></div><div class="tr-hero-visual">${photo(g.photo)}</div></div></section><section class="sec" id="practice"><div class="wrap"><div class="sec-head"><p class="eyebrow">Relevant from the first exercise</p><h2>Work you recognize. Skills you can reuse.</h2></div><div class="tr-examples">${g.examples.map(([t,d],i)=>`<article><span class="tr-index">0${i+1}</span><h3>${t}</h3><p>${d}</p></article>`).join('')}</div><div class="tr-assignment"><p class="eyebrow">A sample class assignment</p><h3>${g.task}</h3><p><strong>Your working takeaway:</strong> ${g.takeaway}</p><p>Use fictional or approved non-sensitive material. Review the result before applying it to your work.${g.slug==='insurance-accounting'?' This class focuses on administrative workflows. It does not provide tax, financial, legal, or insurance advice.':''}</p></div></div></section>${syllabus()}${process()}${band(g.slug)}</div>`;}
export function joinPage(){return `<div class="training"><section class="sec"><div class="wrap tr-join"><div><a class="tr-back" href="${url('/ai-training/')}">← Explore the classes</a>${intro('October 2026 · Join the interest list','Help us form your class.','Tell us about your work and one task you’d like to improve. We’ll bring together 10 people with similar needs, then confirm the class details.')}<div class="tr-join-note"><h2>Interest first. Details next.</h2><ul><li>No payment is collected here.</li><li>Joining does not reserve a seat.</li><li>You decide after seeing the date, format, and price.</li></ul></div><p class="tr-note">Please use general descriptions. Don’t include client records, student data, account numbers, or confidential work.</p></div><div class="tr-form-card"><div id="preview-note" class="tr-form-notice" hidden><strong>Website preview:</strong> You can try this form. No information will be sent or saved.</div><form id="training-interest" action="${SITE.FORMS_ENDPOINT}/contact" method="post"><div class="tr-fields"><label>Your name <span>(required)</span><input name="name" autocomplete="name" required maxlength="120"></label><label>Email <span>(required)</span><input type="email" name="email" autocomplete="email" required maxlength="200"></label><label class="tr-full">Which group fits your work? <span>(required)</span><select name="group" required><option value="">Choose a learning group</option>${GROUPS.map(g=>`<option value="${g.slug}">${g.name}</option>`).join('')}<option value="other">Another field / help me find a match</option></select></label><label>Your role or specialty <span>(required)</span><input name="role" required maxlength="120" placeholder="e.g., solo bookkeeper, agency owner"></label><label>Organization <span>(optional)</span><input name="organization" autocomplete="organization" maxlength="160"></label><label class="tr-full">What would you like AI to help you do? <span>(required)</span><textarea name="goal" rows="3" required minlength="10" maxlength="1200" placeholder="One recurring task is enough."></textarea></label><label>Preferred format<select name="format"><option>No preference yet</option><option>Live online</option><option>In person</option></select></label><label>People potentially interested<select name="team"><option value="1">Just me</option><option value="2-4">2–4 people</option><option value="5-9">5–9 people</option><option value="10+">10 or more people</option></select></label><label class="tr-full">Location, time zone & availability <span>(optional)</span><input name="availability" maxlength="300" placeholder="e.g., Raleigh, Eastern time, weekday evenings"></label><label class="tr-consent tr-full"><input name="consent" type="checkbox" required><span>You may contact me about matching a class and confirming its details. I understand this is an interest list, not registration.</span></label><div class="tr-honeypot" aria-hidden="true"><label>Leave this empty<input name="company" tabindex="-1" autocomplete="off"></label></div></div><p class="tr-note">We use these details to organize classes and follow up with you. No newsletter signup is included. For questions or removal requests, use our <a href="${url('/contact/')}">contact page</a>.</p><button class="btn btn-primary" type="submit" disabled>Join the interest list</button><noscript><p>This form needs JavaScript. Please <a href="${url('/contact/')}">contact LifeQuest AI</a> about class interest.</p></noscript><p id="interest-status" role="status" aria-live="polite" tabindex="-1"></p></form></div></div></section></div>`;}
export function starterPage(){return `<div class="training"><section class="sec"><div class="wrap"><div class="tr-resource-head">${intro('Free AI starter kit · No signup required','Try one useful task today.','25 reusable prompts for everyday work, plus a simple worksheet. Start with a fictional example, review the result, and keep what works.')}<div class="btn-row">${link(url('/resources/ai-starter-kit/25-practice-prompts.txt'),'Download the 25 prompts','primary')}${link(url('/resources/ai-starter-kit/practice-worksheet.txt'),'Download the worksheet','ghost')}<button class="btn btn-ghost" type="button" data-print>Print this guide</button></div></div><div class="tr-assignment"><h2>Use the brief → draft → review routine.</h2><p><strong>Brief:</strong> name the task, audience, facts, and format. <strong>Draft:</strong> ask for a small first version. <strong>Review:</strong> check every fact, remove unsupported claims, and revise before use.</p><p>Replace bracketed placeholders. Use fictional, public, or approved non-sensitive material. If you need current facts, consult original sources; these are writing and practice templates, not verified professional advice.</p></div><nav class="tr-prompt-nav" aria-label="Prompt categories">${[...new Set(PROMPTS.map(p=>p[0]))].map((c,i)=>`<a href="#category-${i}">${esc(c)}</a>`).join('')}</nav>${[...new Set(PROMPTS.map(p=>p[0]))].map((c,i)=>`<section id="category-${i}" class="tr-prompt-category"><h2>${esc(c)}</h2>${PROMPTS.map((p,j)=>p[0]===c?`<details><summary>${String(j+1).padStart(2,'0')} · ${esc(p[1])}</summary><pre id="prompt-${j}">${esc(p[2])}</pre><button class="btn btn-ghost btn-sm" type="button" data-copy="prompt-${j}">Copy prompt</button><p class="tr-note" data-copy-status role="status"></p></details>`:'').join('')}</section>`).join('')}</div></section>${band()}</div>`;}
export function homeLaunch(){return `<div class="training"><section class="tr-hero"><div class="wrap tr-hero-grid"><div class="tr-hero-copy">${intro('Practical AI classes · Targeting October 2026','Your work is the starting point.','Learn AI alongside people with similar work needs. Bring a task you want to make easier, and practice building a useful result you can check and reuse.')}<div class="btn-row">${link(url('/ai-training/'),'Find Your AI Class')}${link(url('/resources/ai-starter-kit/'),'Try the free starter kit','ghost')}</div><p class="tr-note">We organize a class when 10 people with similar needs are interested. Join the list, then decide once the details are confirmed.</p></div><div class="tr-hero-visual">${photo('hero')}<div class="tr-photo-note"><strong>Find your learning group.</strong><span>Business · Insurance & accounting · Education · Nonprofits</span></div></div></div></section></div>`;}
